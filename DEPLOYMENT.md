# Δημοσίευση και λογαριασμοί χρηστών

Ο στόχος: να ανοίγει ο καθένας τη διεύθυνση, να φτιάχνει λογαριασμό με email και
κωδικό, και να βλέπει μόνο τα δικά του οικονομικά.

Το stack είναι το ίδιο που ξέρεις: **Vercel** για τη φιλοξενία, **Supabase** για
βάση και λογαριασμούς. Χρόνος: γύρω στη μιάμιση ώρα την πρώτη φορά.

---

## Βήμα 1. Supabase

1. supabase.com, νέο project, διάλεξε περιοχή **Frankfurt** (πιο κοντά σε Ελλάδα).
2. Ο κωδικός της βάσης που θα σου ζητήσει: κράτησέ τον, δεν εμφανίζεται ξανά.
3. SQL Editor, επικόλλησε ολόκληρο το `supabase/schema.sql`, πάτα Run.

   **Αν η βάση έχει ήδη πίνακες** και δεις σφάλμα τύπου «relation already
   exists», μη χρησιμοποιήσεις το `schema.sql`: σταματά στην πρώτη σύγκρουση και
   δεν τρέχει τίποτα από τα υπόλοιπα. Τρέξε το `supabase/migration.sql`, που
   προσθέτει μόνο ό,τι λείπει και αντέχει να ξανατρέξει.
4. Τρέξε τον έλεγχο που είναι σε σχόλιο στο τέλος του αρχείου. Πρέπει να δεις
   **5 γραμμές με `rowsecurity = true`**. Αν κάποια είναι `false`, σταμάτα και
   ξανατρέξε το κομμάτι των policies. Χωρίς αυτό, ο κάθε χρήστης βλέπει τα
   έξοδα όλων των υπολοίπων.
5. Authentication -> Providers: το **Email** είναι ήδη ενεργό.
6. Authentication -> Settings: αν θέλεις να δοκιμάσεις γρήγορα, απενεργοποίησε
   προσωρινά το "Confirm email". Πριν το δώσεις σε άλλους, ξαναβάλ' το.

Από Settings -> API κράτα δύο πράγματα: το **Project URL** και το κλειδί **anon**.

---

## Βήμα 2. Μεταβλητές περιβάλλοντος

Τοπικά, αρχείο `.env.local` στη ρίζα:

```
VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGci...
```

Το `.env.local` **δεν** ανεβαίνει στο git. Βεβαιώσου ότι υπάρχει στο `.gitignore`.

Για ασφάλεια, τρία πράγματα με μια πρόταση το καθένα:

- Το κλειδί `anon` είναι **δημόσιο** και προορίζεται για το frontend. Οποιοσδήποτε
  μπορεί να το δει στον κώδικα της σελίδας και δεν πειράζει.
- Αυτό που προστατεύει τα δεδομένα είναι τα **RLS policies**, όχι το κλειδί.
- Το κλειδί `service_role` **ποτέ** στο frontend: παρακάμπτει τα RLS.

---

## Βήμα 3. Vercel

1. Ανέβασε τον κώδικα σε repository στο GitHub.
2. vercel.com -> Add New -> Project -> διάλεξε το repository.
3. Το Vercel αναγνωρίζει μόνο του το Vite. Δεν χρειάζεται να αλλάξεις ρυθμίσεις:
   - Build Command: `npm run build`
   - Output Directory: `dist`
4. Environment Variables: πρόσθεσε τις **δύο ίδιες** μεταβλητές του βήματος 2,
   και στα τρία περιβάλλοντα (Production, Preview, Development).
5. Deploy.

Παίρνεις διεύθυνση τύπου `finance-tracker-xxx.vercel.app`. Κάθε `git push` στο
`main` ξανακάνει deploy αυτόματα.

**Ένα σημείο που ξεχνιέται:** πίσω στο Supabase, Authentication -> URL
Configuration, βάλε τη διεύθυνση του Vercel στο **Site URL** και στα **Redirect
URLs**. Χωρίς αυτό, τα email επιβεβαίωσης και επαναφοράς κωδικού στέλνουν τον
χρήστη στο `localhost`.

---

## Βήμα 4. Πώς γίνεται πολυχρηστικό

Η δουλειά είναι ήδη κατά το μεγαλύτερο μέρος καμωμένη, γιατί από την αρχή
βάλαμε `userId` σε κάθε οντότητα και κρύψαμε την αποθήκευση πίσω από interface.

**Στη βάση:** κάθε πίνακας έχει στήλη `user_id` και ένα policy που λέει
«βλέπεις μόνο τις γραμμές όπου `user_id = auth.uid()`». Ο διαχωρισμός γίνεται
στη βάση, όχι στον κώδικα, οπότε δεν υπάρχει περίπτωση να τον ξεχάσεις σε κάποιο
ερώτημα.

**Στην εφαρμογή:** μία γραμμή.

```ts
const repository = createRepository(session?.user.id);
```

Χωρίς session δουλεύει τοπικά στον browser. Με session, όλα πάνε στη Supabase.
Τα components δεν αλλάζουν καθόλου.

**Στην πρώτη σύνδεση** κάθε νέου χρήστη, το `SupabaseRepository` του φτιάχνει το
δικό του αντίγραφο των 36 προεπιλεγμένων κατηγοριών. Έτσι ο καθένας μπορεί να
μετονομάσει το «Σούπερ μάρκετ» σε «Ψώνια» χωρίς να το δει κανένας άλλος.

---

## Βήμα 5. Οθόνη σύνδεσης

Έτοιμη στο `src/features/auth/AuthScreen.tsx`. Μία φόρμα με τρεις καταστάσεις:
σύνδεση, εγγραφή, επαναφορά κωδικού.

Πακέτο που λείπει: `npm install @supabase/supabase-js`

Το μόνο που μένει είναι να τη δέσεις στο `App.tsx`:

```tsx
import { useEffect, useState } from 'react';
import type { Session } from '@supabase/supabase-js';
import { onSessionChange } from './lib/supabase';
import { AuthScreen } from './features/auth/AuthScreen';
import { createRepository } from './store/repository';

export default function App() {
  const [session, setSession] = useState<Session | null>(null);
  const [ready, setReady] = useState(false);

  useEffect(
    () =>
      onSessionChange((s) => {
        setSession(s);
        setReady(true);
      }),
    [],
  );

  // Χωρίς αυτό, φαίνεται για μισό δευτερόλεπτο η οθόνη σύνδεσης
  // σε κάθε ανανέωση, ακόμη κι όταν ο χρήστης είναι ήδη συνδεδεμένος.
  if (!ready) return null;
  if (!session) return <AuthScreen t={t} />;

  const repository = createRepository(session.user.id);
  return <Dashboard {...} />;
}
```

---

## Βήμα 6. Οι προτάσεις μέσω LLM

### Το κλειδί

1. aistudio.google.com, σύνδεση με λογαριασμό Google, **Get API key**.
2. Δωρεάν, χωρίς κάρτα. Το όριο του δωρεάν επιπέδου για το `gemini-2.0-flash`
   είναι της τάξης των μερικών εκατοντάδων αιτημάτων την ημέρα, δηλαδή
   πολλαπλάσιο από ό,τι χρειάζονται δύο άνθρωποι που ανοίγουν την εφαρμογή
   μερικές φορές τη μέρα.
3. Στο Vercel, Environment Variables, πρόσθεσε:
   - `GEMINI_API_KEY` με το κλειδί
   - `VITE_ENABLE_LLM_INSIGHTS` με τιμή `true`

**Προσοχή στο πρόθεμα.** Οι μεταβλητές που ξεκινούν με `VITE_` ενσωματώνονται
στον κώδικα του browser και είναι ορατές σε όλους. Το `GEMINI_API_KEY` σκόπιμα
ΔΕΝ έχει το πρόθεμα, οπότε μένει μόνο στον server.

### Πώς προστατεύεται το κλειδί

Η κλήση στο Gemini δεν γίνεται από τον browser αλλά από τη function
`api/insights.ts` που τρέχει στο Vercel. Τρία επίπεδα:

1. Το κλειδί ζει μόνο σε μεταβλητή περιβάλλοντος του server.
2. Κάθε αίτημα απαιτεί έγκυρο token Supabase. Χωρίς λογαριασμό, απορρίπτεται.
3. Όριο 20 αιτημάτων ανά χρήστη ανά ώρα, ώστε ένας λογαριασμός να μη φάει το
   όριο όλων.

### Τι φεύγει και τι δεν φεύγει

Στέλνονται **μόνο συγκεντρωτικά ανά κατηγορία**: το slug της κατηγορίας, ο μέσος
όρος, το ποσό του μήνα και η τάση. Δεν φεύγουν μεμονωμένες συναλλαγές, ονόματα
καταστημάτων, σημειώσεις, email ή οτιδήποτε ταυτοποιεί πρόσωπο.

Το μοντέλο **δεν κάνει αριθμητική**. Όλοι οι υπολογισμοί γίνονται τοπικά και το
μοντέλο παίρνει έτοιμα νούμερα. Έτσι δεν σου βγάζει ποτέ λάθος ποσά.

Αν το Gemini είναι εκτός λειτουργίας, αργήσει πάνω από 12 δευτερόλεπτα, ή γυρίσει
χαλασμένο JSON, η εφαρμογή επιστρέφει σιωπηλά στους τοπικούς κανόνες. Ο χρήστης
δεν βλέπει ποτέ άδειο Dashboard εξαιτίας του LLM.

---

## Ποιος βλέπει τα δεδομένα σου

Δημόσια διεύθυνση δεν σημαίνει δημόσια δεδομένα. Ο καθένας μπορεί να ανοίξει τη
σελίδα, αλλά βλέπει μόνο την οθόνη σύνδεσης. Τα RLS policies επιβάλλουν στη βάση
ότι κάθε λογαριασμός διαβάζει μόνο τις δικές του γραμμές. Εσύ και η κοπέλα σου
θα έχετε δύο ξεχωριστούς λογαριασμούς και κανείς από τους δύο δεν βλέπει τα
δεδομένα του άλλου.

**Αν θέλεις να μην μπορεί να εγγραφεί κανένας άλλος:** Supabase -> Authentication
-> Providers -> Email, και απενεργοποίησε το **Allow new users to sign up** αφού
φτιάξετε τους δύο λογαριασμούς σας. Η σελίδα μένει προσβάσιμη, αλλά νέες
εγγραφές δεν γίνονται δεκτές.

---

## Κόστος

Και τα τρία (Vercel, Supabase, Gemini) έχουν δωρεάν επίπεδο που καλύπτει άνετα προσωπική χρήση με μερικές
δεκάδες χρήστες. Το μόνο που θέλει προσοχή: **το δωρεάν project της Supabase
παγώνει μετά από μία εβδομάδα χωρίς καμία δραστηριότητα**. Ξυπνάει με ένα αίτημα,
αλλά η πρώτη φόρτωση αργεί.

---

## Σειρά υλοποίησης

1. Supabase project και schema (βήμα 1)
2. Οθόνη σύνδεσης (βήμα 5)
3. Φόρμα καταχώρησης συναλλαγής, ώστε να υπάρχουν πραγματικά δεδομένα
4. Vercel (βήμα 3)
5. Τα υπόλοιπα με ήσυχο κεφάλι
